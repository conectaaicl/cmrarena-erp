# ── Base de datos ─────────────────────────────
DATABASE_URL="postgresql://cmrarena:TuPasswordSeguro@localhost:5432/cmrarena_dev"

# ── Auth JWT ──────────────────────────────────
JWT_SECRET="cambia-esto-a-un-string-de-64-caracteres-aleatorios-muy-secreto"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_SECRET="otro-string-de-64-caracteres-completamente-diferente-al-anterior"
JWT_REFRESH_EXPIRES="7d"

# ── App ───────────────────────────────────────
NODE_ENV="development"
PORT=3000
APP_URL="http://localhost:3000"
FRONTEND_URL="http://localhost:5173"
CORS_ORIGINS="http://localhost:5173"

# ── Email SMTP ────────────────────────────────
MAIL_HOST="mail.conectaai.cl"
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER="noreply@conectaai.cl"
MAIL_PASS="tu-password-smtp"
MAIL_FROM="noreply@conectaai.cl"
MAIL_FROM_NAME="CmrArena ERP"

# ── Almacenamiento ────────────────────────────
UPLOAD_DIR="./uploads"

# ── SII Chile ─────────────────────────────────
SII_CERT_ENCRYPTION_KEY="una-clave-de-exactamente-32-char!"
SII_MOCK_MODE=true

# ── Rate Limiting ─────────────────────────────
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# ── Super Admin ───────────────────────────────
SUPER_ADMIN_EMAIL="superadmin@cmrarena.cl"
SUPER_ADMIN_PASSWORD="ChangeMe!2024"
